spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  #  sql:
  #    init:
  #      mode: never
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true

expirationManager:
  # Custom properties
  default:
    password: 123456
    mailDomain: "@email.email"
  # Cloudinary Properties
  cloudinary:
    cloud-name: cloud_name
    api-key: key
    api-secret: api
# Mailhog properties
mailhog:
  host: localhost
  port: 1025
  username: "expiration@manager.bg"
  password: ""
